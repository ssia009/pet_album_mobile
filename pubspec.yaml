name: petAblumMobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.9
  http: ^1.2.0
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.1.0
  dashed_painter: ^1.1.0
  dotted_border: ^3.1.0
  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  fonts:
    - family: Suit
      fonts:
        - asset: assets/fonts/static/ttf/SUIT-Regular.ttf
          weight: 400
        - asset: assets/fonts/static/ttf/SUIT-Medium.ttf
          weight: 500
        - asset: assets/fonts/static/ttf/SUIT-SemiBold.ttf
          weight: 600

    - family: Dahyun
      fonts:
        - asset: assets/fonts/온글잎 박다현체.ttf

    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard/Pretendard-Regular.otf
        - weight: 400
        - asset: assets/fonts/Pretendard/Pretendard-ExtraBold.otf
          weight: 700

    - family: Kimsonmas
      fonts:
        - asset: assets/fonts/[김씨]손맛체.ttf

    - family: BMKkubulim
      fonts:
        - asset: assets/fonts/BMKkubulim.otf

    - family: BookkMyungjo
      fonts:
        - asset: assets/fonts/BookkMyungjo.ttf

    - family: Cafe24Proup
      fonts:
        - asset: assets/fonts/Cafe24PROUP.otf

    - family: Caveat
      fonts:
        - asset: assets/fonts/Caveat-VariableFont_wght.ttf

    - family: Impact
      fonts:
        - asset: assets/fonts/Impact.ttf

    - family: IndieFlower
      fonts:
        - asset: assets/fonts/IndieFlower.ttf

    - family: MemomentKkukkukk
      fonts:
        - asset: assets/fonts/MemomentKkukkukk.ttf

    - family: Soap
      fonts:
        - asset: assets/fonts/Soap.ttf

    - family: TAEBAEK
      fonts:
        - asset: assets/fonts/TAEBAEK milkyway.otf

    - family: TitanOne
      fonts:
        - asset: assets/fonts/TitanOne.ttf

    - family: YoonChild
      fonts:
        - asset: assets/fonts/YoonChildfundkoreaManSeh.otf


    - family: ZenSerif
      fonts:
        - asset: assets/fonts/ZenSerif.otf


    - family: Goryung
      fonts:
        - asset: assets/fonts/고령딸기체.otf

    - family: Mannyunsul
      fonts:
        - asset: assets/fonts/만년설체.ttf


  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/system/logo/logo.png
    - assets/system/logo/logo.svg
    - assets/system/icons/
    - assets/system/dumy/
    - assets/system/pet_card/
    - assets/system/pet_character/cat/basic_cat/
    - assets/system/pet_character/cat/expression_cat/
    - assets/system/pet_character/cat/faceless_cat/
    - assets/system/pet_character/dog/basic_dog/
    - assets/system/pet_character/dog/expression_dog/
    - assets/system/pet_character/dog/faceless_dog/
    - assets/system/sticker/deco/
    - assets/system/sticker/food/
    - assets/system/sticker/masking_tape/
    - assets/system/sticker/message/
    - assets/system/sticker/nature/
    - assets/system/sticker/note_paper/
    - assets/system/sticker/props/
    - assets/system/sticker/toy/


  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/system/logo/logo.png"  # 이미지 경로
  min_sdk_android: 21
  adaptive_icon_background: "#ffffff"  # Android 어댑티브 아이콘 배경색
  adaptive_icon_foreground: "assets/system/logo/logo.png"
